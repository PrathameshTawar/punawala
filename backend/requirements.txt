# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1
aiosqlite==0.20.0       # async SQLite driver (dev/test)
asyncpg==0.29.0         # async PostgreSQL driver (production)
psycopg2-binary==2.9.9  # sync PostgreSQL driver (Alembic migrations)

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# API utilities
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic-settings==2.2.1
pydantic-settings==2.2.1

# AI / ML
openai>=1.14.0,<2.0.0     # 1.x API — AsyncOpenAI client
mediapipe==0.10.14
opencv-python-headless==4.9.0.80
numpy==1.26.4

# Observability
structlog==24.1.0
prometheus-client==0.19.0

# OpenTelemetry (optional — only needed if OTEL_SERVICE_NAME is set)
opentelemetry-sdk==1.23.0
opentelemetry-instrumentation-fastapi==0.44b0
opentelemetry-exporter-otlp-proto-grpc==1.23.0

# Resilience
tenacity==8.2.3

# Redis (optional — for distributed rate limiting)
redis==5.0.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0

# Load testing
locust==2.20.0
pydantic==2.7.1
